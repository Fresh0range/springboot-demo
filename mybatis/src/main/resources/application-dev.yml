spring:
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/demo-mybatis?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
  devtools:
    restart:
      enabled: true #设置开启热部署
      additional-paths: src/main/java #重启目录
server:
  port: 8080 #端口

mybatis: #mybatis配置
  type-aliases-package: org.jzy.demo.mybatis.entity